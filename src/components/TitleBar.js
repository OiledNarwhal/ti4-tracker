import './TitleBar.css';

function TitleBar() {

    return (
        <div className="titlebar">
            <h1>Twilight Imperium 4 - Game Tracker</h1>
        </div>
    );
}

export default TitleBar;